# AI Dossier — Lite

Attach this completed file to your submission. The rule: more than 10 content lines total and the dossier is ungraded.
A "content line" is a non-empty line you write inside a field — headings, `<!-- hint -->` lines, and blank lines don't count.
Check before you push: `./check_dossier.sh DOSSIER_LITE.md`

## Model and settings

<!-- hint: model name + version + any non-default settings, one or two lines -->
(your entry)

## Best prompt (verbatim)

<!-- hint: paste the ONE prompt that earned its keep, byte-for-byte — no cleanup -->
(your entry)

## The mistake I caught (symptom → evidence → fix)

<!-- hint: symptom (what looked wrong), evidence (the output or diagnostic that proves it), fix (what you changed) -->
(your entry)
